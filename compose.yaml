services:
  driftbot:
    build: .
    restart: unless-stopped
    init: true
    env_file: .env
    volumes:
      - drift-data:/data
    stop_grace_period: 35m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
volumes:
  drift-data:
